프로토타입 패턴

- 기존 인스턴스를 복제하여 새로운 인스턴스를 만드는 방법

 - 복제 기능을 갖추고 있는 기존 인스턴스를 프로토타입으로 사용해 새 인스턴스를 만들 수 있음.

 - 기존의 인스턴스를 만들때 시간이 오래걸리는 경우

 - Client가 prototype(clone메서드)(자바에서는 제공) 사용하여 concretePrototype A B C... 의 clone 메서드

 - 기존 인스턴스 안의 데이터를 모두 복제하여 원하는 값만 변경하여 사용하면 비교적 효율적

 - 장점
    > 복잡한 객체를 만드는 과정을 숨길 수 있다.
    > 기존 객체를 복제하는 과정이 새 인스턴스를 만드는 것보다 비용적인 측면에서 효율적일 수 있음
    > 반드시 해당 클래스와 동일할 필요는 없음. 좀 더 추상적인 타입을 리턴할 수 있음.

 - 단점
    > 복잡한 객체를 만드는 과정 자체가 복잡할 수 있다.(특히, 순환 참조가 있는 경우)